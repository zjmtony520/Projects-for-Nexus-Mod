# Projects-for-Nexus-Mod

A Place to store my code. I code using AI.

## Submitting a pull request from this repo

Quick steps:

1. Commit your changes locally.
2. Push the branch to your fork (e.g., `git push origin work`).
3. Open the PR on GitHub.

## Building the Pet Bond mod DLL

For step-by-step instructions to compile `PetBondMod` into a SMAPI-ready `PetBondMod.dll` and copy the manifest and localization into the output, see [`PetBondMod/README.md`](PetBondMod/README.md).
